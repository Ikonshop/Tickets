import { useEffect, useState } from "react"
import { useConnection, useWallet } from "@solana/wallet-adapter-react"

export const SoldTickets = (req) => {
    //Displays all Sold Tickets From the Event


    return (
        <div>
            <h1>Sold Tickets</h1>
        </div>
    )
}
